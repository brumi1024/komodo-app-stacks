name: "autokuma-vps"

services:
  autokuma:
    env_file:
      - .env
    labels:
      - homepage.group=Monitoring
      - homepage.name=AutoKuma VPS
      - homepage.icon=uptime-kuma.svg
      - homepage.description=Auto-managed Kuma monitors (homelab-vps)
