name: "caddy-sequoia"

services:
  caddy:
    container_name: caddy-sequoia
    labels:
      - homepage.name=Caddy Sequoia
      - homepage.description=Sequoia Reverse Proxy
networks:
  default:
    name: caddy-network
    enable_ipv6: true
